# =============================================================================
# SISCOM — Configuración del entorno Docker
# Modifica estos valores antes de ejecutar docker compose up
# =============================================================================

# ── InfluxDB ──────────────────────────────────────────────────────────────────
INFLUX_ADMIN_USER=admin
INFLUX_ADMIN_PASSWORD=siscom2026
INFLUX_ORG=agricultura
INFLUX_BUCKET_RAW=agro_iot_data
INFLUX_BUCKET_IND=agro_iot_indicadores
INFLUX_BUCKET_ALE=agro_iot_alertas
# Token fijo de administrador — no cambiar una vez iniciado (se persiste en el volumen)
INFLUX_TOKEN=siscom-agro-2026-influxdb-admin-token

# ── Grafana ───────────────────────────────────────────────────────────────────
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=siscom2026

# ── Notificaciones (opcionales — solo necesarias para el servicio de alertas) ─
SMTP_USER=administradorparcelasiscom@gmail.com
SMTP_PASSWORD=otstuboougzttpnw
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=

# ── Simulador ─────────────────────────────────────────────────────────────────
# Duración de la simulación en segundos (0 = indefinido hasta Ctrl+C)
SIMULADOR_DURACION=3600
# Factor de aceleración: 60 = 1 día virtual por minuto real
FACTOR_ACELERACION=60
